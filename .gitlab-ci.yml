# GitLab Pages Deployment Configuration
pages:
  stage: deploy
  script:
    - mkdir .public
    - cp -r * .public 2>/dev/null || true
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - main
